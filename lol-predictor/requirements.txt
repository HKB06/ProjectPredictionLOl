# ─────────────────────────────────────────────────────────────────────────────
# Versions ÉPINGLÉES aux versions du venv LOCAL où l'app tourne sans erreur.
# Pourquoi : sur le cloud, uv retéléchargeait les dernières versions à chaque
# déploiement (ex. streamlit 1.59.1, numpy bridé 2.2.x) -> combo jamais testé ->
# "Segmentation fault" (librairie compilée incompatible avec les autres).
# On fige donc le combo EXACT validé en local. NE PAS élargir sans re-tester le
# déploiement cloud (reboot + logs sans segfault).
# ─────────────────────────────────────────────────────────────────────────────

# Framework + UI
streamlit==1.58.0
altair==6.2.1

# Stack scientifique compilée (source des segfaults si versions mal alignées)
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.8.0
lightgbm==4.6.0
pyarrow==24.0.0
matplotlib==3.10.9

# Utilitaires (pur Python, aucun risque de segfault)
pyyaml==6.0.3
gdown==6.1.0
requests==2.34.2

# Assistant IA (page 5) — non installé en local, borne large volontaire
anthropic>=0.40
python-dotenv>=1.0
# Barre de chat multimodale : coller des images (Ctrl+V) directement dans le chat de l'assistant
st-chat-input-multimodal>=2.0
