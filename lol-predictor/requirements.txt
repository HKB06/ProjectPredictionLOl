# Bornes hautes : le cloud (uv) installait pandas 3.0 / numpy 2.5 (bleeding-edge,
# breaking) -> on reste sur les majeures testées localement pour un déploiement stable.
pandas>=2.2,<3
numpy>=1.26,<2.3
# 1.9.0 bloqué par Windows Smart App Control (DLL _cyutility) ; 1.7.x embarque
# ce meme _cyutility (conflit ABI Cython intermittent) -> on fixe la 1.8.x
scikit-learn>=1.8,<1.9
lightgbm>=4.3
scipy>=1.11
pyarrow>=15
matplotlib>=3.8
pyyaml>=6.0
streamlit>=1.49
# Automatisation : téléchargement data (Drive) + récup calendrier matchs (Leaguepedia)
gdown>=5.1
requests>=2.31
# Assistant IA (page Streamlit) : agent Claude (tool use + vision) sur nos données
anthropic>=0.40
python-dotenv>=1.0
