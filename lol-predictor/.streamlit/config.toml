[browser]
gatherUsageStats = false

[server]
# IMPORTANT déploiement cloud : headless=true sinon Streamlit affiche l'invite e-mail
# au 1er lancement et ATTEND une saisie clavier -> le serveur ne démarre pas (health
# check 'connection refused', écran "Oh no"). En local le navigateur s'ouvre via le .bat.
headless = true
